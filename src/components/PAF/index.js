import React from 'react';
import { Typography, Box } from '@mui/material';

const PAF = () => {
  return (
    <Box>
      <Typography variant="h5">PAF Form</Typography>
    </Box>
  );
};

export default PAF;
